[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serve index.html for all routes so react-router-dom works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
